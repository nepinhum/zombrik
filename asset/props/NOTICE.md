# asset/props — CC0

Props cut from `../packs/city` (Kenney, CC0): trees, cars, lamps, skips, barrels,
barricades, benches, crates, bins and rocks.

This is the palette maps place from in Tiled. Add a prop by dropping a trimmed
PNG here and adding it to the map's `props` tileset.
